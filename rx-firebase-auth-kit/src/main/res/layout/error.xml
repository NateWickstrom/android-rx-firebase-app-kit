<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:tools="http://schemas.android.com/tools"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              android:id="@+id/error"
              android:layout_width="match_parent"
              android:layout_height="@dimen/error_min_height"
              android:layout_marginTop="@dimen/error_margin"
              android:layout_marginBottom="@dimen/error_margin"
              android:gravity="center_vertical"
              android:orientation="horizontal">

    <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_margin="8dp"
            android:src="@drawable/ic_baseline_error_24px"
            android:tint="@color/red"/>

    <TextView
            android:id="@+id/error_massage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/red"
            android:ellipsize="end"
            tools:text="thin is a long long long long long long long long long long long long long long long error message"/>

</LinearLayout>